<script src="./pwa.ts"></script>

<div
  id="pwa-toast"
  class="pointer-events-none fixed right-4 bottom-4 z-50 hidden w-[min(26rem,calc(100vw-2rem))] rounded-2xl border border-white/10 bg-slate-900/95 p-4 text-sm text-slate-200 shadow-2xl shadow-black/40"
  role="alert"
  aria-live="polite"
>
  <div class="mb-3">
    <p class="font-medium text-slate-100" id="toast-title">NotMyNet</p>
    <p class="mt-1 text-slate-400" id="toast-message"></p>
  </div>

  <div class="flex gap-2">
    <button
      id="pwa-refresh"
      class="hidden rounded-lg bg-sky-500 px-3 py-2 font-medium text-slate-950 transition hover:bg-sky-400"
      type="button"
    >
      Reload
    </button>

    <button
      id="pwa-close"
      class="rounded-lg border border-white/10 bg-slate-800 px-3 py-2 font-medium text-slate-200 transition hover:bg-slate-700"
      type="button"
    >
      Close
    </button>
  </div>
</div>
